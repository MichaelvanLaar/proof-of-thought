# Keep CLAUDE.md config file table in sync
node scripts/sync-config-table.js

# Run linter on staged files
npm run lint

# Run type check
npm run typecheck

# Run tests
npm test -- --run --reporter=verbose
