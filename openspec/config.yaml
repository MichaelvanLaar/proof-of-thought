schema: spec-driven

context: |
  Tech stack: TypeScript 5, Node.js 18+, Z3 solver (native + WASM), OpenAI SDK
  Build: esbuild (ESM + CJS + browser bundles)
  Testing: Vitest with >80% coverage target
  We use Conventional Commits with gitmoji
  Domain: Neurosymbolic reasoning library combining LLMs with formal theorem proving
  Package: @michaelvanlaar/proof-of-thought (npm, public)
